# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['CAN_TestFrame.py'],
    pathex=[],
    binaries=[],
    datas=[('CAN_TestFrame_TOOL使用说明书.html', '.'), ('logo_cat.png', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='CAN_TestFrame_TOOL',
    icon='logo_cat.ico',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
